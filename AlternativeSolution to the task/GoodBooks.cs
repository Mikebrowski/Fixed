using DocumentFormat.OpenXml.Spreadsheet;
using DocumentFormat.OpenXml.Vml.Office;
using Microsoft.Data.SqlClient;
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Threading.Tasks;
using static Microsoft.EntityFrameworkCore.DbLoggerCategory.Database;
using Connection = Microsoft.EntityFrameworkCore.DbLoggerCategory.Database.Connection;

namespace knowit.AlternativeSolution_to_the_task
{
    public class GoodBooks
    {
        /*

        foreach(DataRow row in dt.Rows)
        {
                DataTable dt = new DataTable(); //DATABASE CONNECTION
                DataColumn dc = new DataColumn();
                SqlBulkCopy sqlBulk = new SqlBulkCopy(Connection);

           sqlBulk.DestinationTableName = "GodeBoker";

            sqlBulk.ColumnMappings.Add("Id", "Id");
            sqlBulk.ColumnMappings.Add("Tittle", "Tittle");
            sqlBulk.ColumnMappings.Add("ISBN", "ISBN");
            sqlBulk.ColumnMappings.Add("Pris", "Pris");
            sqlBulk.ColumnMappings.Add("Antall", "Antall");
            sqlBulk.WriteToServer(dt);
        }
        */

        /*
        public int dager;
        
        if(dager == 1)
            {
             console.write(mondag)
             datatable da new
             antall
            }

        */
    }
}
